import pageStyles from '../styles/Page.module.scss';

const contact = () => {
    return (
        <div className={pageStyles.container}>
            <h1 className={pageStyles.h1}>Contact</h1>
        </div>
    )
}

export default contact;
